pfSquid is an XMLRPC server to manage for pfSense Squid and squidGuard through a web interface. The difference in pfSquid instead use the existing interface in pfsense pfSquid is that the configuration can be shared among multiple servers. Example: so no need to connect to multiple servers to allow or block a site